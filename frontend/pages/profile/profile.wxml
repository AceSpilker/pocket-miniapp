<view class="profile-page page-enter" style="{{cssVars}}">

  <!-- 动态背景 -->
  <view class="bg-shapes">
    <view class="shape shape-1"></view>
  </view>

  <!-- 头像区 -->
  <view class="avatar-section fade-in-up">
    <view class="avatar-wrap tap-scale" bindtap="handleChooseAvatar">
      <image
        class="avatar-img"
        src="{{avatarUrl || '/assets/images/default-avatar.png'}}"
      />
      <view class="avatar-edit">
        <text>📷</text>
      </view>
    </view>
    <text class="avatar-name">{{nickname || i18n.texts['profile.noNickname']}}</text>
  </view>

  <!-- 基本信息表单 -->
  <view class="form-card glass-card fade-in-up" style="animation-delay: 0.1s;">
    <view class="card-title">📝 {{i18n.texts['profile.basicInfo']}}</view>
    <van-cell-group>
      <van-field
        value="{{ nickname }}"
        label="{{i18n.texts['profile.nickname']}}"
        placeholder="{{i18n.texts['profile.nicknamePlaceholder']}}"
        bind:change="onNicknameInput"
      />
      <van-field
        value="{{ phone }}"
        label="{{i18n.texts['profile.phone']}}"
        placeholder="{{i18n.texts['profile.optional']}}"
        bind:change="onPhoneInput"
      />
      <van-field
        value="{{ email }}"
        label="{{i18n.texts['profile.email']}}"
        placeholder="{{i18n.texts['profile.optional']}}"
        bind:change="onEmailInput"
      />
    </van-cell-group>

    <view class="btn-group">
      <van-button
        type="primary"
        block
        round
        loading="{{saving}}"
        bind:click="handleSave"
      >
        💾 {{i18n.texts['profile.save']}}
      </van-button>
    </view>
  </view>

  <!-- 提示消息 -->
  <van-notice-bar
    wx:if="{{successMsg}}"
    color="#10b981"
    background="rgba(16, 185, 129, 0.1)"
    text="{{successMsg}}"
    wrapable
  />
  <van-notice-bar
    wx:if="{{errorMsg && !successMsg}}"
    color="#ef4444"
    background="rgba(239, 68, 68, 0.1)"
    text="{{errorMsg}}"
    wrapable
  />
</view>
